<!doctype html>
<html>
<head>
    <meta charset="UTF-8" />
    <link href="{{ app.request.basepath }}/stylesheets/bootstrap.css" rel="stylesheet">
    <link href="{{ app.request.basepath }}/stylesheets/app.css" rel="stylesheet">
    <title>{% block title %}{% endblock %} - Admin</title>
</head>
<body>
    <div class="container">
        <header class="page-header">
            <p>
                <a href={{ path('admin.index') }}>
                    <img src="{{ app.request.basepath }}/images/logo.png">
                </a>
            </p>
            <h2 class="text-primary">Emine Hakan - Ostéopathe D.O.<br><small>Gestion des articles</small></h2>
        </header>

        {% block content %}{% endblock %}


        {% if app.user and is_granted('IS_AUTHENTICATED_FULLY') %}
        <hr>

        <footer class="panel">
            <div class="panel-footer">
                Vous êtes connecté en tant que {{ app.user.username }} - <a href="{{ path('logout') }}">Se déconnecter</a>
            </div>
        </footer>
        {% endif %}
    </div>

    <script type="text/javascript" src="{{ app.request.basepath }}/scripts/jquery-3.1.1.min.js"></script>
    <script type="text/javascript" src="{{ app.request.basepath }}/scripts/jquery-ui.min.js"></script>
    <script type="text/javascript" src="{{ app.request.basepath }}/scripts/bootstrap.min.js"></script>
    <script type="text/javascript" src="{{ app.request.basepath }}/scripts/app.js"></script>
</body>
</html>
